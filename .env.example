# Copia este archivo a `.env` en la raíz del repositorio y rellena tus claves.
# `.env` está en .gitignore — no se versiona.
#
# En macOS/Linux (sesión actual):
#   export OPENAI_API_KEY="sk-..."
#   export DEEPSEEK_API_KEY="sk-..."

OPENAI_API_KEY=
DEEPSEEK_API_KEY=
