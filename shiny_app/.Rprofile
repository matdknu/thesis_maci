# Configuración para shinyapps.io
# Este archivo se ejecuta antes de cargar la app

# Configurar opciones de carga de datos
options(shiny.maxRequestSize = 100*1024^2)  # 100MB max

# Mensaje de inicio
cat("=== Shiny App - Análisis Político Reddit ===\n")
cat("Cargando configuración...\n")










